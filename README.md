# BrandOS - Генератор контента для бренда

Простое веб-приложение для создания контента на основе профиля вашего бренда с использованием Google Gemini AI.

## 🚀 Быстрый старт

### 1. Установка зависимостей

Убедитесь, что у вас установлен Python 3.8 или выше. Затем установите необходимые библиотеки:

```bash
pip install -r requirements.txt
```

### 2. Настройка API ключа

1. Получите API ключ от Google Gemini: https://makersuite.google.com/app/apikey
2. Создайте файл `.env` в корне проекта
3. Добавьте в файл следующую строку:

```
GEMINI_API_KEY=ваш_ключ_здесь
```

**Важно:** Не публикуйте файл `.env` в интернет! Он уже добавлен в `.gitignore`.

### 3. Запуск приложения

```bash
streamlit run app.py
```

Приложение откроется автоматически в браузере по адресу `http://localhost:8501`

## 📖 Как использовать

### Шаг 1: Заполните профиль бренда

Перейдите на вкладку **"Профиль бренда"** и заполните информацию о вашей компании:
- Название компании
- Описание деятельности
- Целевая аудитория
- Тональность общения
- Ценности бренда
- Ключевые сообщения

Нажмите **"Сохранить профиль"** - данные сохранятся локально в файл `brand_profile.json`.

### Шаг 2: Генерируйте контент

После заполнения профиля доступны три модуля:

1. **Брейншторм** - AI придумает креативные идеи для контента
2. **Генератор постов** - Создайте готовый пост на любую тему
3. **Контент-план** - Получите план публикаций на неделю или месяц

## 📁 Структура проекта

```
BrandOS/
├── app.py              # Главный файл приложения (интерфейс)
├── brand_data.py       # Работа с данными о бренде
├── ai_engine.py        # Интеграция с Google Gemini
├── requirements.txt    # Зависимости проекта
├── .env                # API ключ (создайте сами)
├── brand_profile.json  # Сохраненный профиль (создается автоматически)
└── README.md           # Этот файл
```

## 🔧 Требования

- Python 3.8+
- Интернет-соединение (для работы с Gemini API)
- API ключ от Google Gemini

## 💡 Советы

- Чем подробнее вы заполните профиль бренда, тем лучше AI будет понимать ваш стиль
- Экспериментируйте с разными темами и форматами постов
- Сохраняйте понравившиеся идеи и посты вручную (функция копирования доступна)

## ⚠️ Важно

- Все данные хранятся локально на вашем компьютере
- API ключ хранится в файле `.env` - не публикуйте его
- Для работы требуется активное интернет-соединение

## ☁️ Деплой на Streamlit Cloud (Рекомендуется)

Деплой в облако решает проблему с VPN и дает доступ к приложению из любой точки мира по ссылке.

### Преимущества Streamlit Cloud:
- ✅ Работает без VPN (серверы в США)
- ✅ Бесплатный хостинг
- ✅ Автоматическое обновление при изменении кода
- ✅ Доступ по ссылке с любого устройства

### Пошаговая инструкция:

#### Шаг 1: Подготовка репозитория на GitHub

1. Создайте аккаунт на [GitHub](https://github.com), если его еще нет
2. Создайте новый репозиторий (например, `BrandOS`)
3. Загрузите код в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш_username/BrandOS.git
   git push -u origin main
   ```

**Важно:** Убедитесь, что файл `.env` или `env.local` **НЕ** попал в репозиторий (он уже в `.gitignore`)

#### Шаг 2: Подключение к Streamlit Cloud

1. Перейдите на [share.streamlit.io](https://share.streamlit.io)
2. Войдите через GitHub (нажмите "Sign in with GitHub")
3. Нажмите кнопку **"New app"**
4. Заполните форму:
   - **Repository:** выберите ваш репозиторий `BrandOS`
   - **Branch:** `main`
   - **Main file path:** `app.py`
   - **App URL:** выберите уникальное имя (например, `brandos`)

#### Шаг 3: Добавление API ключа в Streamlit Cloud

1. В настройках приложения найдите раздел **"Secrets"** (или "Advanced settings")
2. Нажмите **"New secret"**
3. Добавьте секрет с именем `GEMINI_API_KEY` и значением вашего API ключа:
   ```
   GEMINI_API_KEY=ваш_ключ_здесь
   ```
4. Сохраните

#### Шаг 4: Запуск

1. Нажмите **"Deploy"** или **"Save"**
2. Streamlit Cloud автоматически установит зависимости из `requirements.txt`
3. Через 1-2 минуты ваше приложение будет доступно по ссылке:
   ```
   https://brandos.streamlit.app
   ```
   (или другое имя, которое вы выбрали)

### Обновление приложения

После каждого изменения кода:
1. Закоммитьте изменения в GitHub:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
2. Streamlit Cloud автоматически обновит приложение (обычно за 30-60 секунд)

### Безопасность

- ✅ API ключ хранится в секретах Streamlit Cloud (безопасно)
- ✅ Файл `.env` не попадает в репозиторий
- ✅ Приложение доступно только по вашей уникальной ссылке

## 🆘 Проблемы?

### Локальный запуск:
1. Проверьте, что установлены все зависимости: `pip install -r requirements.txt`
2. Убедитесь, что файл `.env` или `env.local` создан и содержит правильный API ключ
3. Проверьте подключение к интернету

### Деплой на Streamlit Cloud:
1. Убедитесь, что все файлы загружены в GitHub
2. Проверьте, что `requirements.txt` содержит все зависимости
3. Убедитесь, что секрет `GEMINI_API_KEY` добавлен в настройках Streamlit Cloud
4. Проверьте логи в панели управления Streamlit Cloud (кнопка "Manage app")

---

**BrandOS MVP** - Создано для простоты и эффективности 🚀
